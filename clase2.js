var nombre = 'Adrian', apellido = 'Bautista'

var nombreEnMinuscula = nombre.toUpperCase()
var apellidoEnMinuscula = apellido.toLowerCase()

var cantidadNombre = nombre.length
var posicionLetraNombre = nombre.charAt(2)

var str = nombre.substr(3,2)

var nombreCompleto = `${nombre} ${apellido.toUpperCase()}`

//Reto de la ultima letra de mi nombre
var ultimaLetraNombre = nombre.charAt(nombre.length - 1)